# Lines starting with `#` are comments.
# https://riptutorial.com/fr/git/example/885/ignorer-les-fichiers-et-les-repertoires-avec-un-fichier--gitignore

# Ignoring directories
# Both the directory itself and its contents will be ignored.
node_modules/

# Ignoring files by extension
# All files with these extensions will be ignored in
# this directory and all its sub-directories.
*.bak
.gitignore

# Environment file that contains parameters to connect to the ldap server
# It is used by the "dotenv" module
# A model of this file is available : .env.example
.env
